<!DOCTYPE html>
<html lang="de">

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>CHECK24 Search</title>
    <style>
        html {
            box-sizing: border-box;
        }

        *,
        *:before,
        *:after {
            box-sizing: inherit;
        }

        body {
            font-family: Arial, Helvetica, sans-serif;
        }

        .info {
            color: #005ea8;
        }

        #container {
            width: 100vw;
            padding-right: 20px;
            display: flex;
            flex-direction: column;
            gap: 10px;
        }

        .c {
            overflow: hidden;
            flex-grow: 1;
            border: 2px solid #005ea8;
            border-radius: 10px;
            display: flex;
            padding: 20px;
        }

        #betrag {
            font-family: 'Courier New', Courier, monospace;
            color: #005ea8;
            font-size: 2rem;
        }

        p {
            width: 100%;
            text-align: right;
        }

        input {
            padding-left: 3px;
            border: 2px solid #005ea8;
            border-radius: 10px;
            height: 40px;
            width: 300px;
        }

        #orderDiv {
            width: 100%;
            height: 45px;
            background-color: #005ea8;
            border-radius: 30px;
            padding-top: -20px;
        }
    </style>

</head>

<body>
<div style="display: flex; justify-content: space-between; align-items: center; padding: 30px">
    <h1 style="font-family: Avenir">
        Searching for: <%= q %>
    </h1>
    <form action="search">
        <input type="text" name="q" id="searchInput" placeholder="Enter search term">
    </form>
</div>
<div id="orderDiv">

</div>
<div id="container">
    <% table.forEach(function(item) { %>
        <div class="c">
            <img src="<%= item.image %>">
            <div style="flex-grow: 1">
                <p><%= item.name %></p>
                <p id="betrag"><%= item.price %> €</p>
            </div>
        </div>
    <% }); %>
</div>
</body>
</html>